import React from 'react';
import { CardContainer, CardTitle } from '.';

function Card() {
  return (
    <CardContainer>
      <CardTitle>card</CardTitle>
    </CardContainer>
  );
}

export default Card;
